# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Dart

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      # Note: This workflow uses the latest stable version of the Dart SDK.
      # You can specify other versions if desired, see documentation here:
      # https://github.com/dart-lang/setup-dart/blob/main/README.md
      - uses: dart-lang/setup-dart@v1
      #- uses: dart-lang/setup-dart@9a04e6d73cca37bd455e0608d7e5092f881fd603
      - name: Set up Flutter 
        uses: subosito/flutter-action@v2
        with: 
            flutter-version: '3.10.6'
      - name: Install dependencies
        run: flutter pub get

      # Uncomment this step to verify the use of 'dart format' on each commit.
      # - name: Verify formatting
      #   run: dart format --output=none --set-exit-if-changed .

      # Consider passing '--fatal-infos' for slightly stricter analysis.
      - name: Analyze project source
        run: dart analyze
      - run: flutter build apk
      - uses: actions/upload-artifact@v1
        with:
          name: app-release.apk
          path: build/app/outputs/apk/release/app-release.apk
      - uses: actions/checkout@v3
      #- name: Install flutter connect
      #  run: sudo  npm install -g ./sauce-connect-launcher
      - uses: saucelabs/sauce-connect-action@v2.3.1
        with:
          username: ${{ secrets.SAUCE_USERNAME }}
          accessKey: ${{ secrets.SAUCE_ACCESS_KEY }}
          tunnelName: oauth-naveen.gurram-f50a5_tunnel_name
          configFile: ${{ github.workspace }}/sc -c %HOMEPATH%\sc\config.yml
         # ./sc -u oauth-naveen.gurram-f50a5 -k c5810172-2661-4e8f-954c-d639ff8f0f29 --region eu-central --tunnel-name oauth-naveen.gurram-f50a5_tunnel_name
            # ...

    #  - uses: actions/upload-artifact@v3
    #    if: ${{ failure() }}
    #    with:
    #      name: sauce-connect-log
    #      path: ${{ env.SAUCE_CONNECT_DIR_IN_HOST }}/sauce-connect.log
